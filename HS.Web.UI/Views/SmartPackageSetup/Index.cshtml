
@{
    Layout = "~/Views/Shared/_PrivateLayout.cshtml";
}
<script>
    $(document).ready(function () {
        $(".LoaderWorkingDiv").hide();
    })
</script>
@{
    string url = Request.Url.AbsolutePath;

    if (url.ToLower().IndexOf("/smartpackagesettings") == 0)
    {
        ViewBag.Title = "Smart Package Settings";
        Html.RenderAction("SmartPackageSettingsPartial", "SmartPackageSetup");
    }
    //if (url.ToLower().IndexOf("/leadverifyinfo") == 0)
    //{
    //    ViewBag.Title = "Lead Verify Info";
    //    Html.RenderAction("VerifyLeadPartial", "Leads");
    //}
    //if (url.ToLower().IndexOf("/leadsetupinfo") == 0)
    //{
    //    ViewBag.Title = "Lead Setup Info";
    //    Html.RenderAction("LeadSetupPartial", "Leads");
    //}
}
