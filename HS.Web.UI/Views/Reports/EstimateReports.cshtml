@{ 
    Layout = null;
}
@using Localize = HS.Web.UI.Helper.LanguageHelper
@using PermissionChekcer = HS.Web.UI.Helper.PermissionHelper
@using Permissions = HS.Framework.UserPermissions

<script>

    String.prototype.replaceAll = function (search, replacement) {
        var target = this;
        return target.split(search).join(replacement);
    };
    var TabsLoaderText = "<div class='invoice-loader'><div><div class='lds-css ng-scope'><div style='margin:auto; z-index:99;' class='lds-double-ring'><div></div><div></div></div></div></div></div>";

    var DataTablePageSize = 50;
    var PayrollStartDatepicker;
    var PayrollEndDatepicker;

    var LoadEstimatesReportTab = function () {
        var StartDate = $(".min-date").val();
        var EndDate = $(".max-date").val();
        $(".Load_Estimates_Report").html(TabsLoaderText);
        $(".Load_Estimates_Report").load(domainurl + "/Reports/LoadEstimateReportPartial");
    }
     
    $(document).ready(function () {
        $(".LoaderWorkingDiv").hide();
        LoadEstimatesReportTab();


        $(".EstimateReportsReportTab").click(function () {
            LoadEstimatesReportTab();
        });
    });
</script>

<link href="~/Content/Css/Report/LeadsReportPartial.css" rel="stylesheet" />
<link href="~/Content/Css/Report/ReportsPartial.css" rel="stylesheet" />
<div class="ReportsTitle">
    @Localize.T("Reports")
</div>


<div class="col-xs-12 Report-Tabs">
    <div class="nav-detail-contents">
        <ul class="nav nav-tabs payroll_report_top_nav" id="myTab">
            <li class="EstimateReportsReportTab active">
                <a idval="" data-target="#EstimateReport" data-toggle="tab">@Localize.T("Estimate")</a>
            </li>
        </ul>
        <div class="tab-content reports_tab_content">
            <div class="tab-pane active" id="EstimateReport">
                <div class="row leads_report_top_bar">
                    <div class="Load_Estimates_Report">

                    </div>
                </div>

            </div>
        </div>
    </div>
</div>
