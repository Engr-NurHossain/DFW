@{
    Layout = "~/Views/Shared/_PrivateLayout.cshtml";
}
@using AppConfig = HS.Framework.Utils.AppConfig;

@Scripts.Render("~/scripts/Report")

@{ 
    string DomainSitePath = AppConfig.DomainSitePath;
    string url = Request.Url.AbsolutePath.ToLower();
    if (url.IndexOf(DomainSitePath + "/reports") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("ReportsPartial", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/leadsreports") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("LeadsReports", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/customerreports") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("CustomerReports", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/techinventoryreports") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("TechInventoryReports", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/expensereports") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("ExpenseReports", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/payrollreports") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("PayrollReports", "Reports");
    }

    else if (url.IndexOf(DomainSitePath + "/reportupload") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("ReportUpload", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/ticketreports") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("TicketReports", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/technicianreports") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("TechnicianReports", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/installationtrackerreport") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("InstallationTrackerReport", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/csractivityreport") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("CSRActivityReport", "Reports");
    }

    else if (url.IndexOf(DomainSitePath + "/assignarticle-reports") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("AllRemindersReportList", "Reports");
    }

    else if (url.IndexOf(DomainSitePath + "/inventoryreports") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("InventoryReports", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/salesreports") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("SalesReports", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/brinksreport") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("BrinksReport", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/uccreport") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("UccReport", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/bookingsalesreport") == 0)
    {
        ViewBag.Title = "BookingReports";
        Html.RenderAction("BookingSalesReports", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/jobsreports") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("jobsReports", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/jobcostingreports") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("JobCostingReports", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/rmrreports") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("RMRReports", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/historyreports") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("HistoryReports", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/conversionreports") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("conversionReports", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/hudsonleadsreport") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("HudsonLeadsReportPartial", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/activitiesreport") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("ActivitiesReportPartial", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/accountsreport") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("AccountsReportPartial", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/opportunitiesreport") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("OpportunitiesReportPartial", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/softbacklogreport") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("SoftBacklogReportPartial", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/hardbacklogreport") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("HardBacklogReportPartial", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/cancellationcue") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("CancellationCue", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/salesmatrixreports") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("SalesMatrixReports", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/hrreports") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("HrReports", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/partnerreports") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("PartnerReports", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/leadsourcereports") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("LeadSourceReports", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/upsellsreports") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("UpsellsReports", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/agingreport") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("AgingReport", "Reports");
    }
    else if (url.IndexOf(DomainSitePath + "/estimatereports") == 0)
    {
        ViewBag.Title = "Reports";
        Html.RenderAction("EstimateReports", "Reports");
    }

}