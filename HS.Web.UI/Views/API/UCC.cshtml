@using PermissionChekcer = HS.Web.UI.Helper.PermissionHelper
@using Permissions = HS.Framework.UserPermissions
@model HS.Entities.Customer
@{
    Layout = null;
}
@using Localize = HS.Web.UI.Helper.LanguageHelper
<script>
    var UCCRefId = '@Model.UCCRefId';
    $(".CreateCustomerUCC").click(function () {
        OpenTopToBottomModal(String.format("/API/AddUCCCustomer?CustomerId={0}", CustomerLoadGuid))
    });

</script>
<link href="~/Content/Css/AlarmDotCom/alarm.css" rel="stylesheet" />
<script src="~/Content/Js/UCC/Ucc.js"></script>

<div class="width-100p create_alarm">
    @if (string.IsNullOrEmpty(Model.UCCRefId))
    {
        <div class="customersyncarea">
            <label>Transmitter Code</label>
            <div class="input-group add-on">
                <input type="text" id="UCCSyncId" title="Transmitter Code" />
                @*@if (PermissionChekcer.IsPermitted(Permissions.CustomerPermissions.UCCSyncCustomer))
                {*@
                    <div class="input-group-btn"> <button class="btn btn-default syncbtnUCC">@Localize.T("Sync")</button></div>
                @*}*@
                </div>
        </div>
        <div class="CreateCustomerDiv width-100p">
            <button class="btn CreateCustomerUCC"><i class="fa fa-user-plus"></i> @Localize.T("Create Customer")</button>

            @if (PermissionChekcer.IsPermitted(Permissions.CustomerPermissions.UccSettings))
            {
                <button class="btn btn-default edituccsettingsbtn"><i class="fa fa-cog"></i></button>

            }
        </div>
    }
    else
    {
<div class="CreateCustomerDiv width-100p">
    @if (PermissionChekcer.IsPermitted(Permissions.CustomerPermissions.UccSettings))
    {
        <button class="btn btn-default edituccsettingsbtn"><i class="fa fa-cog"></i></button>

    }


</div>
        <div id="UCCCustomerDetailsLoad">

        </div>
    }
</div>