@*@model IEnumerable<HS.Entities.CustomerNote>*@
@{
    Layout = null;
}
@using PermissionChekcer = HS.Web.UI.Helper.PermissionHelper
@using Permissions = HS.Framework.UserPermissions
@using Localize = HS.Web.UI.Helper.LanguageHelper
@using HS.Framework
<link href="~/Content/Css/CustomerDetails/CustomerAccountActivityDetails.css" rel="stylesheet" />
<script>
    $(document).ready(function () {
        //$('[data-toggle="tooltip"]').tooltip();
    });
</script>
    <div class="ci_block ci_margin_top">
        <div class="ci_header">
            @Localize.T("Orders")

            @*@if (ViewBag.CustomerDetailNoteReminderPlusButton == true)
            {
                <button class="btn add_notes_reminder green-tooltip" data-toggle="tooltip" title="@Localize.T("Add Notes/Reminder")" onclick="NewNotesLoad('fromdetails')"><i class="fa fa-plus"></i></button>
            }*@

            
        </div>
        <div class="ci_info note_box_container">
            
           
               <span>Orders Block</span>
              
                    @*@foreach (var item in Model)
                    {
        <div class="customer_note_box_details">
                        <div class="info-table-tr note_text_color">
                           
                                    @if (item.IsFollowUp != true && item.IsShedule != true)
                                    {
                                        if (item.IsPin == true)
                                        {
                                            <div title="@Html.Raw(item.Notes)"> <i class="fa fa-sticky-note-o" style="color:#2CA01C;"></i> @Html.Raw(item.Notes)</div>
                                        }
                                        else
                                        {
                                            <div title="@Html.Raw(item.Notes)"> <i class="fa fa-sticky-note-o"></i> @Html.Raw(item.Notes)</div>
                                        }

                                    }
                                    else
                                    {
                                        <div title="@Html.Raw(item.Notes)"><i class="fa fa-clock-o"></i> @Html.Raw(item.Notes)</div>
                                    }
                               
                           
                            </div>
                           <div class="info-table-tr notes_padd">
                               <div class="tr-title">
                                   <div class="note-color" style="background-color:#@item.Color">

                                   </div>
                                   <div>@item.NoteTypeValue</div>
                               </div>
                               <div class="tr-data note_text_right">
                               On @item.CreatedDate.UTCToClientTime().ToString("MM/dd/yy hh:mm tt") by
                                   <div class="user_profile_pic_div">
                                       <img src="/EmpShow/W50H50X@(item.CreatedByUid)" />
                                   </div>
                                   @item.empName
                            </div>
                        </div>
                            
        </div>
                    }*@
               
        </div>
    </div>
