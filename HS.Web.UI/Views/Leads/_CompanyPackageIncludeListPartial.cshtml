@{
    Layout = null;
}
@using Localize = HS.Web.UI.Helper.LanguageHelper
@model List<HS.Entities.PackageInclude>

<link href="~/Content/Css/PackageSettings/PacakgeIncludeListPartial.css" rel="stylesheet" />
<script src="~/Content/Js/PackageSetup/PackageIncludeList.js"></script>

<div class="packageIncludeListTableContainer">
    <table id="packageIncludeListTable">
        <thead>
            <tr>
                <th>@Localize.T("Equipment Name")</th>
                <th>@Localize.T("Package Name")</th>
                <th>@Localize.T("Number of Equipment")</th>
                <th>@Localize.T("Action")</th>
            </tr>
        </thead>

        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td><a class="edit-package-include" idval="@item.Id">@item.IncludedEquipmentName</a></td>
                    <td>@item.IncludedPackageName</td>
                    <td>@item.EptNo</td>
                    <td><a class="delete-package-include" idval="@item.Id" title="@Localize.T("Delete")"><i class="fa fa-trash-o"></i></a></td>
                </tr>
            }
        </tbody>
    </table>
</div>
